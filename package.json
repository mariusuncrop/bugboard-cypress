{
  "name": "bugboard-cypress",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress end-to-end tests for the BugBoard demo app.",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "cypress run",
    "test:chrome": "cypress run --browser chrome",
    "test:firefox": "cypress run --browser firefox",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "axe-core": "^4.13.0",
    "cypress": "^16.1.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
